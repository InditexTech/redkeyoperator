apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    control-plane: redkey-operator-webhook
  name: redkey-operator-webhook
  namespace: redkey-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      control-plane: redkey-operator-webhook
  template:
    metadata:
      labels:
        control-plane: redkey-operator-webhook
        domain: DOMAIN
        environment: ENVIRONMENT
        layer: middleware-redkeyoperator-webhook
        slot: default
        tenant: TENANT
        type: middleware
    spec:
      containers:
      - command:
        - /webhook
        image: redkey-operator-webhook
        imagePullPolicy: Always
        name: redkey-operator-webhook
        ports:
        - containerPort: 9443
          name: webhook-server
          protocol: TCP
        resources:
          limits:
            cpu: "1"
            memory: 1Gi
          requests:
            cpu: "0.3"
            memory: 200Mi
        securityContext:
          allowPrivilegeEscalation: false
        volumeMounts:
        - mountPath: /tmp/k8s-webhook-server/serving-certs
          name: cert
          readOnly: true
      securityContext:
        runAsNonRoot: true
      serviceAccountName: redkey-operator-sa
      terminationGracePeriodSeconds: 10
      volumes:
      - name: cert
        secret:
          defaultMode: 420
          secretName: redkey-operator-webhook-service-cert
